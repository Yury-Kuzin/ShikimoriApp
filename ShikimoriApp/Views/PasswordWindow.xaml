<Window x:Class="ShikimoriApp.Views.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShikimoriApp.Views"
        xmlns:vm="clr-namespace:ShikimoriApp.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:PasswordWindowViewModel}"
        mc:Ignorable="d"
        Title="Ввод пароля" Height="96" Width="245">
    <Grid>
        <PasswordBox x:Name="passwordBox" HorizontalAlignment="Center" VerticalAlignment="Center" Width="185" Height="41" FontSize="22" vm:PasswordBoxHelper.Attach="True" vm:PasswordBoxHelper.Password="{Binding Model.Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <PasswordBox.InputBindings>
                <KeyBinding Key="Return" Command="{Binding CheckPassword}" CommandParameter="{Binding ElementName=passwordBox}" />
            </PasswordBox.InputBindings>
        </PasswordBox>
        <Label Visibility="Hidden" Content="Пароль введён неверно!" HorizontalAlignment="Center" VerticalAlignment="Top" Height="16" Width="185" FontSize="8" Padding="0,0,5,5" Margin="0,6,0,0" Foreground="#FFF72B2B"/>

    </Grid>
</Window>
