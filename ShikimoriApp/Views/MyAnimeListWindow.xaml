<Window x:Class="ShikimoriApp.Views.MyAnimeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShikimoriApp.Views"
        mc:Ignorable="d"
        Title="MyAnimeListWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Column="0" Header="Тип списка" Padding="6" Margin="15,10,15,323">
            <StackPanel>
                <RadioButton Height="20" Content="Запланированно" GroupName="ListType" IsChecked="True"/>
                <RadioButton Height="20" Content="Смотрю" GroupName="ListType"/>
                <RadioButton Height="20" Content="Просмотренно" GroupName="ListType"/>
                <RadioButton Height="20" Content="Брошено" GroupName="ListType"/>
            </StackPanel>
        </GroupBox>
        <DataGrid Grid.Column="1">

        </DataGrid>

    </Grid>
</Window>
